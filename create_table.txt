--TABLES

CREATE TABLE ACTOR (
    ACTOR_ID 				NUMBER(3),
    NAME 					VARCHAR2(50) NOT NULL,
    SURNAME					VARCHAR2(50) NOT NULL,
    GENDER					VARCHAR2(10),
    EMAIL					VARCHAR2(50) NOT NULL,
    CITY					VARCHAR2(50),
    COUNTRY					VARCHAR2(50) NOT NULL,
    DOB						DATE NOT NULL
);

CREATE TABLE EPISODE(
    EPISODE_ID 				NUMBER(3),
    TITLE					VARCHAR2(100) NOT NULL,
    SEASON					NUMBER(3) NOT NULL,
    LENGTH					NUMBER(5)
);


CREATE TABLE SERIES(
    SERIES_ID				NUMBER(3),
    TITLE					VARCHAR2(100) NOT NULL,
    DESCRIPTION				VARCHAR2(500),
--  ACTOR_ID				NUMBER(3),
    PROD_YEAR				NUMBER(4) NOT NULL
);

CREATE TABLE CASTING(
    ACTOR_ID				NUMBER(3) NOT NULL,
    SERIES_ID				NUMBER(3) NOT NULL,
    ROLE 					VARCHAR2(20) NOT NULL
);



CREATE TABLE VIEWER (
    VIEWER_ID 				NUMBER(9),
    NAME					VARCHAR2(50)  NOT NULL,
    SURNAME					VARCHAR2(50) NOT NULL,
    GENDER					CHAR(1),
    EMAIL					VARCHAR2(50) NOT NULL,
    CITY					VARCHAR2(50),
    COUNTRY					CHAR(2) DEFAULT 'US',
    ADDRESS					VARCHAR2(50),
    DOB						DATE,
);

CREATE TABLE VIEWERSHIP (
    VIEWER_ID			    NUMBER(9) NOT NULL,
    EPISODE_ID				NUMBER(9) NOT NULL,
    DOV		                DATE,
);
-----------------------------------------------------------

--Sequences

CREATE SEQUENCE seq_actor_id
INCREMENT BY 1
NO CYCLE;

CREATE SEQUENCE seq_episode_id
INCREMENT BY 1
NO CYCLE;

CREATE SEQUENCE seq_series_id
INCREMENT BY 1
NO CYCLE;

CREATE SEQUENCE seq_viewers_id
INCREMENT BY 1
NO CYCLE;


